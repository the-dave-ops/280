# Vite Configuration
# Allowed hosts for Vite dev server (comma-separated)
# Add your custom domain here to allow access from that domain
# IMPORTANT: After changing this, restart the Vite dev server!
VITE_ALLOWED_HOSTS=280.kesef-kesef.com,localhost,.localhost

# API Configuration
# ================

# For local development (HTTP - direct access):
# VITE_API_URL=http://localhost:3001

# For production with Nginx proxy and separate API subdomain (RECOMMENDED):
# Frontend: https://280.kesef-kesef.com
# Backend API: https://api.280.kesef-kesef.com
VITE_API_URL=https://api.280.kesef-kesef.com

# Alternative: Same domain with port (if not using nginx):
# VITE_API_URL=https://280.kesef-kesef.com:3001
