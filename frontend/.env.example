# Vite Configuration
# Allowed hosts for Vite dev server (comma-separated)
# Example: VITE_ALLOWED_HOSTS=280.kesef-kesef.com,localhost,.localhost
VITE_ALLOWED_HOSTS=localhost,.localhost

# API Configuration
VITE_API_URL=http://localhost:3001
