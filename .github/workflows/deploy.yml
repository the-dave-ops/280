name: Deploy to Production

on:
  push:
    branches:
      - main  # Change this to your production branch name
  workflow_dispatch:  # Allows manual trigger from GitHub UI

jobs:
  deploy:
    name: Deploy to Remote Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            cd ${{ secrets.PROJECT_PATH }}
            
            # Fix git ownership issue
            git config --global --add safe.directory ${{ secrets.PROJECT_PATH }}
            sudo chown -R $USER:$USER ${{ secrets.PROJECT_PATH }}
            
            echo "Stopping containers..."
            sudo docker compose down
            
            echo "Ensuring HTTPS remote..."
            git remote set-url origin https://github.com/the-dave-ops/280.git
            
            echo "Pulling latest changes..."
            git pull origin main
            
            echo "Building and starting containers..."
            sudo docker compose up --build -d
            
            echo "Deployment completed successfully!"
